{
  "name": "god-brave",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "balance": "node scripts/balance.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
