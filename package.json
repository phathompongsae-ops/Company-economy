{
  "name": "company-economy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Company Economy — business strategy prototype. Core simulation is headless; Three.js renders only.",
  "scripts": {
    "simulate": "node src/sim/run.js",
    "test": "node tests/run-tests.mjs && node tests/coco-regression-tests.mjs",
    "serve": "npx --yes serve -l 8080 ."
  },
  "devDependencies": {
    "three": "0.160.0"
  },
  "_vendorNote": "ui/vendor/three.module.js is checked in verbatim from three@0.160.0/build/three.module.js so the hot-seat UI has zero external CDN dependency at runtime."
}
